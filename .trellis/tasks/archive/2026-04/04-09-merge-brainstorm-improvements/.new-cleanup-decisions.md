# .new Cleanup Decisions

## Scope

This record captures the file-by-file decision for reviewed `.new` candidates during task `04-09-merge-brainstorm-improvements`.

Total reviewed candidates: `17`

Decision vocabulary:

- `merged`: useful content was manually merged into the canonical file
- `skipped`: candidate was rejected because it would regress project-specific behavior or introduce incorrect guidance
- `deleted`: the `.new` candidate file was removed after the decision above

---

## Decisions

| Candidate | Canonical file | Decision | Reason |
|-----------|----------------|----------|--------|
| `.agents/skills/brainstorm/SKILL.md.new` | `.agents/skills/brainstorm/SKILL.md` | `merged` + `deleted` | absorbed useful requirement-discovery method improvements while preserving project-specific gates |
| `.claude/commands/trellis/brainstorm.md.new` | `.claude/commands/trellis/brainstorm.md` | `merged` + `deleted` | same as above, with cross-CLI parity preserved |
| `.agents/skills/check/SKILL.md.new` | `.agents/skills/check/SKILL.md` | `skipped` + `deleted` | would downgrade task-level multi-CLI review gate into a generic lint/typecheck checklist |
| `.claude/commands/trellis/check.md.new` | `.claude/commands/trellis/check.md` | `skipped` + `deleted` | would remove reviewer gate / reviewer command generation flow |
| `.agents/skills/finish-work/SKILL.md.new` | `.agents/skills/finish-work/SKILL.md` | `skipped` + `deleted` | used incorrect `pnpm type-check`, referenced missing spec paths, and dropped Trellis sync rules |
| `.claude/commands/trellis/finish-work.md.new` | `.claude/commands/trellis/finish-work.md` | `skipped` + `deleted` | same regression pattern as skill variant |
| `.agents/skills/onboard/SKILL.md.new` | `.agents/skills/onboard/SKILL.md` | `skipped` + `deleted` | removed project-specific hidden-directory sync reminder |
| `.claude/commands/trellis/onboard.md.new` | `.claude/commands/trellis/onboard.md` | `skipped` + `deleted` | removed project-specific hidden-directory sync reminder |
| `.agents/skills/record-session/SKILL.md.new` | `.agents/skills/record-session/SKILL.md` | `skipped` + `deleted` | incorrectly promoted `add_session.py` over `record-session-helper.py` for final close-out |
| `.claude/commands/trellis/record-session.md.new` | `.claude/commands/trellis/record-session.md` | `skipped` + `deleted` | same regression pattern as skill variant |
| `.claude/commands/trellis/start.md.new` | `.claude/commands/trellis/start.md` | `skipped` + `deleted` | would remove project-specific Phase Router and change-management routing |
| `.trellis/config.yaml.new` | `.trellis/config.yaml` | `skipped` + `deleted` | comment change would point users away from final close-out helper workflow |
| `.trellis/scripts/add_session.py.new` | `.trellis/scripts/add_session.py` | `skipped` + `deleted` | removed staged/unstaged safety checks in workspace auto-commit logic |
| `.trellis/scripts/common/developer.py.new` | `.trellis/scripts/common/developer.py` | `skipped` + `deleted` | rewrote developer guidance to prefer `add_session.py` over helper-based final close-out |
| `.trellis/scripts/common/git.py.new` | `.trellis/scripts/common/git.py` | `skipped` + `deleted` | removed porcelain helper functions now used by safer metadata auto-commit logic |
| `.trellis/scripts/common/task_store.py.new` | `.trellis/scripts/common/task_store.py` | `skipped` + `deleted` | mixed an unfinished task-phase model expansion with removal of git safety checks |
| `.trellis/worktree.yaml.new` | `.trellis/worktree.yaml` | `skipped` + `deleted` | deleted current project-specific verification matrix notes |

---

## Result

- Total reviewed `.new` candidates: `17`
- All reviewed `.new` candidates have been either merged into canonical files or explicitly skipped and deleted.
- No `.new` files remain in the repository after the final cleanup pass.
